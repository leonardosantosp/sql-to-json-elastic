# sql-to-json-elastic
This project reads a database with over 500k records, transforming it into clean, structured JSON. It uses regular expressions to remove unwanted characters, preparing the data for Elasticsearch indexing. Ideal for migrating SQL data to Elasticsearch while cleaning and standardizing it for better search performance.
